# Object-Recognition-Using-YOLOv8-and-OpenCV
A Python project that performs object recognition on static images using YOLOv8 and OpenCV. The program detects multiple objects, draws bounding boxes with labels, displays the detection results, and automatically saves the processed images for easy analysis and visualization.
